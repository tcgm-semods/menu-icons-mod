# Menu Icons

Replaces the bottom-right HUD row (Menu, Minimap, Squad, Galaxy, plus the
Menu button's dropup) with one row of nine direct icon buttons -- no
dropup, tooltip on hover, red dot for pending notifications.

Icons are Feather Icons (MIT), pre-rendered to PNG at build time and
tinted to match the HUD's accent color.
